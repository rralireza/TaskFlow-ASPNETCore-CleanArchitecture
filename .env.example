# Must satisfy SQL Server's password complexity policy.
MSSQL_SA_PASSWORD=ChangeThisSqlPassword_123!

# Use at least 32 random characters in production; never commit the real value.
JWT_KEY=replace-with-a-long-random-secret-at-least-32-characters
JWT_ISSUER=TaskFlowAPI
JWT_AUDIENCE=TaskFlowClient
